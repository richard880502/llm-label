# server.py 使用 MCP 1.x 的 FastMCP import 路徑；2.x 已移除該模組。
mcp>=1.2.0,<2.0.0
# FastAPI 0.115 pins Starlette below 0.42; keep the MCP transport dependency
# in its compatible major line instead of letting pip backtrack across every
# sse-starlette 3.x release during image builds.
sse-starlette>=1.6.1,<2.0.0
httpx>=0.27.0
